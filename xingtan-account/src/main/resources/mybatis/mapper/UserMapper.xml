<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xingtan.account.mapper.UserMapper" >
    <resultMap id="UserMap" type="com.xingtan.account.entity.User" >
        <id column="id" property="id"/>
        <result column="userName" property="user_name"/>
        <result column="realName" property="real_name"/>
        <result column="sex" property="sex"/>
        <result column="birthday" property="birthday"/>
        <result column="telephone" property="telephone"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="gmtCreate" property="gmt_create"/>
        <result column="gmtModified" property="gmt_modified"/>
    </resultMap>

    <sql id="BASE_COLUMNS" >
        <![CDATA[id, user_name, real_name, sex, birthday, telephone, email, `password` ]]>
    </sql>
    <sql id="ALL_COLUMNS" >
        <![CDATA[id, user_name, real_name, sex, birthday, telephone, email, `password`, gmt_create, gmt_modified]]>
    </sql>

    <select id="getAll" resultMap="UserMap"  >
        SELECT
        <include refid="ALL_COLUMNS" />
        FROM `user`
    </select>

    <select id="getUserById" parameterType="java.lang.Long" resultMap="UserMap" >
        SELECT
        <include refid="ALL_COLUMNS" />
        FROM `user`
        WHERE id = #{id}
    </select>
    <select id="getUserByUserName" parameterType="java.lang.String" resultMap="UserMap" >
        SELECT
        <include refid="ALL_COLUMNS" />
        FROM `user`
        WHERE user_name = #{userName}
    </select>
    <select id="getUserByPhone" parameterType="java.lang.String" resultMap="UserMap" >
        SELECT
        <include refid="ALL_COLUMNS" />
        FROM `user`
        WHERE `telephone` = #{telephone}
    </select>
    <select id="getUserByEmail" parameterType="java.lang.String" resultMap="UserMap" >
        SELECT
        <include refid="ALL_COLUMNS" />
        FROM `user`
        WHERE email = #{email}
    </select>
    <insert id="insertUser" parameterType="com.xingtan.account.entity.User" >
        INSERT INTO
        `user`
        (user_name, real_name, sex, birthday, telephone, email, `password`, gmt_create, gmt_modified)
        VALUES
        (#{userName}, #{realName}, #{sex}, #{birthday}, #{telephone}, #{email}, #{password}, NOW(), NOW())
    </insert>

    <update id="updateUser" parameterType="com.xingtan.account.entity.User" >
        UPDATE
        `user`
        SET
        <if test="userName != null">user_name = #{userName},</if>
        <if test="realName != null">real_name = #{realName},</if>
        <if test="sex != null">sex = #{sex},</if>
        <if test="birthday != null">birthday = #{birthday},</if>
        <if test="email != null">email = #{email},</if>
        <if test="password != null">password = #{password},</if>
        gmt_modified = now()
        WHERE
        id = #{id}
    </update>

    <delete id="deleteUser" parameterType="java.lang.Long" >
        DELETE FROM
        `user`
        WHERE
        id =#{id}
    </delete>

</mapper>